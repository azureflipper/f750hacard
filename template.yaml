template:
  - sensor:
      - name: "Nibe heating priority"
        state: >
          {% if is_state('sensor.nibe_68392_43064', 'unavailable') %}
            water
          {% else %}
            heating
          {% endif %}
